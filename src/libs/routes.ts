/**
 * All the Application routes are to be defined here.
 * so in future, any changes in App routes can be adjusted here
 */
export const routes = {
  home: () => "/",
  users: () => "/users",
  products: () => "/products",
  // ...add more urls here
};
